---
name: deploy-service
description: "Design, implement, review, or troubleshoot secure host-side deployments for static, Python, C++/CUDA, hybrid, native, or container services. Covers approved data/www roots, immutable releases, restricted activation, health gates, rollback, retention, TLS, DNS, and firewall safety; use service-cicd for GitHub Actions automation."
---

# /deploy-service

Read and follow the canonical vendor-neutral skill at:

```text
.agents/skills/deploy-service/SKILL.md
```

Load only the references selected by that body for the current host deployment
model and project profile. Load `/service-cicd` as well when GitHub Actions CI,
auto-deployment, or auto-release is in scope.
