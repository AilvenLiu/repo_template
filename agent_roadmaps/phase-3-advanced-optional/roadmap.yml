phase: 3
name: Advanced Optional

status:
  active: false
  blocked: false
  started_at: null
  completed_at: null

depends_on_phases: [phase-2-ai-infra-content]

tasks:
  - id: task-3-1
    title: Author autotuning frameworks constraint as draft
    description: >
      ACTIVATION GATE: do not start until the user names a consuming project
      that requires this content. Once activated, write a draft constraint
      covering AutoTVM, Ansor, MetaSchedule (TVM autotuners), Triton's
      autotuner, and FlashInfer's JIT compile flow. Cover when to autotune,
      tuning database lifecycle, reproducibility, CI integration, and
      cross-machine portability of tuned configurations. Marked status: draft.
      Validate against the named consuming project before marking complete.
    status: pending
    effort: high
    key_files:
      - .ai/constraints/cpp/autotuning.md
    depends_on: []
    acceptance:
      - User has named a consuming project in a session handoff.
      - Constraint file exists with status: draft.
      - Each named framework is covered with a selection-criteria block.
      - Validation evidence against the named project is recorded.

  - id: task-3-2
    title: Author tvm-ffi calling convention constraint as draft
    description: >
      ACTIVATION GATE: do not start until the user names a consuming project
      that requires this content (e.g., a TVM Unity or Relax fork). Once
      activated, write a draft constraint specific to tvm-ffi's
      packed-function ABI: PackedFunc, TVMValue, type codes, NDArray and
      DLTensor flow, the C ABI, registration patterns, and how it differs
      from pybind11 or nanobind. Cross-reference Phase 2's ffi-boundary.md
      for the generic FFI rules. Marked status: draft.
    status: pending
    effort: high
    key_files:
      - .ai/constraints/hybrid/tvm-ffi.md
    depends_on: []
    acceptance:
      - User has named a consuming project in a session handoff.
      - Constraint file exists with status: draft.
      - PackedFunc, TVMValue, type codes, NDArray flow each have a section.
      - Cross-reference to ffi-boundary.md is explicit.
      - Validation evidence against the named project is recorded.

  - id: task-3-3
    title: Author LLVM-as-a-library linking constraint as draft
    description: >
      ACTIVATION GATE: do not start until the user names a consuming project
      that requires this content (e.g., TVM Relay/Relax LLVM AOT codegen,
      MLC LLVM-based compilation). Once activated, write a draft constraint
      covering LLVM as a build dependency, version compatibility (LLVM 14,
      15, 16, 17, 18 against CUDA toolkit versions), shared vs static
      linking, ORC JIT vs MCJIT vs the legacy JIT, symbol export discipline
      for embedded LLVM, and the cmake recipe. Marked status: draft.
    status: pending
    effort: high
    key_files:
      - .ai/constraints/cpp/llvm-linking.md
    depends_on: []
    acceptance:
      - User has named a consuming project in a session handoff.
      - Constraint file exists with status: draft.
      - LLVM version compatibility matrix is explicit.
      - Shared vs static linking trade-offs are documented.
      - cmake recipe is included.
      - Validation evidence against the named project is recorded.

  - id: task-3-4
    title: Author HIP/ROCm parallel-stack constraint as draft
    description: >
      ACTIVATION GATE: do not start until the user names a consuming project
      that requires this content (e.g., MLC-LLM ROCm backend, FlashInfer
      ROCm port, TVM ROCm target). Once activated, write a draft constraint
      covering hipify workflow, the parallel HIP source tree, build-time
      backend selection (CUDA vs ROCm), HSA error handling, ROCm toolchain
      discovery, and the kernel-correctness test extension for ROCm.
      Cross-reference Phase 2's cuda-modern.md and kernel-correctness.md.
      Marked status: draft.
    status: pending
    effort: high
    key_files:
      - .ai/constraints/cpp/hip-rocm.md
    depends_on: []
    acceptance:
      - User has named a consuming project in a session handoff.
      - Constraint file exists with status: draft.
      - hipify workflow is documented.
      - Build-time backend selection pattern is explicit.
      - Cross-references to cuda-modern.md and kernel-correctness.md are present.
      - Validation evidence against the named project is recorded.

  - id: task-3-5
    title: Author WebGPU/Metal/SPIR-V codegen constraint as draft
    description: >
      ACTIVATION GATE: do not start until the user names a consuming project
      that requires this content (e.g., MLC-LLM WebLLM, TVM WebGPU target,
      Metal-backed inference). Once activated, write a draft constraint
      covering WGSL generation, MSL generation, SPIR-V generation, the
      browser-side runtime, the Metal Performance Shaders bridge, the
      SPIR-V validator, and the cross-backend correctness rubric.
      Marked status: draft.
    status: pending
    effort: high
    key_files:
      - .ai/constraints/cpp/wgpu-metal-spirv.md
    depends_on: []
    acceptance:
      - User has named a consuming project in a session handoff.
      - Constraint file exists with status: draft.
      - WGSL, MSL, SPIR-V generation each have a section.
      - Cross-backend correctness rubric is explicit.
      - Validation evidence against the named project is recorded.

focus:
  current_task: null
  notes: >
    Phase remains pending and intentionally may stay dormant indefinitely.
    On 2026-05-12, Phase 2 completion status was reconciled and the
    phase branch roadmap/phase-3-advanced-optional was created from master.
    No Phase 3 task was activated because no consuming project and defined
    validation time window have yet been named in a session handoff.
    Activation requires user approval naming a real consuming project per
    task. See INVARIANTS.md section 3.
