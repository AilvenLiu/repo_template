# Session Handoff: Task-1-6 Complete

**Date:** 2026-05-10  
**Task:** task-1-6 - Refactor bin/agent-dependency to dispatch by profile  
**Status:** ✅ Completed  
**Branch:** roadmap/phase-1-profile-architecture

## What Was Done

Refactored the dependency management system to dispatch by `build_system` axis from `project_profile` instead of legacy `project_type`.

### Changes to `.ai/scripts/dependency/utils.py`

1. **Updated imports:**
   - Changed from `project_type` to `project_profile` module
   - Import `ProjectProfile`, `detect`, and `BuildSystem`
   - Kept `ProjectType` import for backward compatibility

2. **Added `detect_project_profile()` method:**
   ```python
   def detect_project_profile(self) -> ProjectProfile:
       """Detect project profile using shared module."""
       return detect_project_profile(self.repo_root)
   ```

3. **Updated `detect_project_type()` for backward compatibility:**
   - Maps `profile.build_system` to legacy `ProjectType` enum
   - poetry → PYTHON
   - cmake/bazel → CPP
   - scikit-build → PYTHON (hybrid, but Python-primary)

### Changes to `.ai/scripts/dependency/add.py`

1. **Updated imports:**
   - Added `BuildSystem` to imports from utils

2. **Renamed function:**
   - `add_cpp_dependency()` → `add_cpp_dependency_cmake()`
   - Updated docstring to clarify CMake-specific behavior

3. **Added stub functions for new build systems:**
   - `add_dependency_scikit_build_stub()` - prints "not yet implemented" message
   - `add_dependency_bazel_stub()` - prints "not yet implemented" message
   - `add_dependency_mixed_stub()` - prints "not yet implemented" message
   - All stubs reference ADR and Phase 2 roadmap, exit with code 1

4. **Updated `main()` dispatch logic:**
   - Changed from `project_type = manager.detect_project_type()` to `profile = manager.detect_project_profile()`
   - Dispatch now uses `build_system = profile.build_system`
   - Updated print statements: "Project Type" → "Build System"
   - Added branches for scikit-build, bazel, and mixed
   - Updated error messages to reference build systems

### Test Coverage

Created `.ai/scripts/tests/test_agent_dependency.py` with 8 tests:

**TestDependencyManagerDispatch (5 tests):**
- `test_detect_poetry_from_profile` - Verifies poetry detection
- `test_detect_cmake_from_profile` - Verifies cmake detection
- `test_detect_scikit_build_from_profile` - Verifies scikit-build detection
- `test_detect_bazel_from_profile` - Verifies bazel detection
- `test_detect_mixed_from_profile` - Verifies mixed detection

**TestDependencyBackwardCompatibility (3 tests):**
- `test_legacy_python_maps_to_poetry` - Verifies project_type: python → poetry
- `test_legacy_cpp_maps_to_cmake` - Verifies project_type: cpp → cmake
- `test_profile_takes_precedence_over_legacy` - Verifies profile overrides legacy

## Verification

### Test Results
```
$ python -m pytest .ai/scripts/tests/ -v
============================== 77 passed in 4.65s ===============================
```

All tests pass:
- 18 project_profile tests
- 8 project_type shim tests
- 10 session_init tests
- 25 capability_audit tests
- 8 agent_build tests
- 8 agent_dependency tests (NEW)

### Backward Compatibility

✅ Legacy `project_type: python` maps to `build_system: poetry`  
✅ Legacy `project_type: cpp` maps to `build_system: cmake`  
✅ New `project_profile.build_system` takes precedence over legacy field  
✅ All existing poetry and cmake code paths unchanged  
✅ Stub branches for new build systems exit non-zero with helpful messages

## Commits

1. `88cc0fa` - feat(architecture): refactor agent-dependency to dispatch by build_system
   - Refactored utils.py and add.py
   - Added stub functions for new build systems
   - Updated dispatch logic to use build_system

2. `ca7fa02` - test(architecture): add tests for agent-dependency dispatch logic
   - Added 8 tests for build system detection
   - Fixed import in utils.py (detect aliased as detect_project_profile)
   - All 77 tests passing

## Next Steps

Task-1-7 is now active: Add migration notes to template overlays (CLAUDE.md and AGENTS.md files).

## Notes

- The dependency management system now uses the six-axis profile schema
- Poetry and cmake paths remain exactly as they were before
- New build systems (scikit-build, bazel, mixed) have stub implementations
- Phase 2 will implement the actual backends for new build systems
- All changes maintain complete backward compatibility with legacy project_type
