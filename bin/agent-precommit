#!/bin/bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

python3 "$REPO_ROOT/.ai/tools/policy_gate.py" --op mutate --context '{}'
exec python3 "$REPO_ROOT/.claude/skills/pre-commit/scripts/validate.py"
