---
name: dependency
description: Add dependencies via repository policy wrappers (Poetry for Python, Conan/vcpkg for C++).
---

# Codex Dependency

Run:

```bash
bin/agent-dependency add <package> [version] [--dev]
```
